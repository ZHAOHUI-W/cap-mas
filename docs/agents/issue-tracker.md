# Issue Tracker

CAP-MAS uses local Markdown issues under `.scratch/<feature>/` because the
project is nested in the parent repository and the GitHub CLI is unavailable in
the current environment. Issues use a `Status:` line and are published beside
the spec. The canonical ready label is `ready-for-agent`.
