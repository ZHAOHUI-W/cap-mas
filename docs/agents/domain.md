# Domain Documentation Rules

CAP-MAS is a single-context project. Domain decisions live in `docs/`, and
architecture decisions live in `docs/adr/`. Agents should read the relevant ADR
and the project glossary before changing contracts or runtime authority.
